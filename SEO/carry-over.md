# Carry-over ledger

Open items that survive across weekly reports. `scripts/seo_diff.py` reads this table,
computes weeks-open from `first_raised`, and escalates anything at 3+ weeks to 🔴.

The ledger exists because G5 ("count carry-overs and escalate") was aspirational without
state — it depended on someone carefully re-reading last week's prose. KAB-1721 sat open for
five weeks and only resurfaced because a July file happened to get read. Silent carry-over is
how the queue rots.

**Rules**
- One row per open item. `first_raised` is the date it was *first* recommended, not last mentioned.
- `status`: `blocked` (waiting on a human) · `open` (actionable now) · `watching` (monitor only)
- Move completed items to Closed with the version that shipped them. Don't delete — a closed
  item is the evidence that stops it being re-proposed.
- Keep the pipe table intact; the parser is deliberately simple.

## Open

| id | item | first_raised | owner | status | notes |
|---|---|---|---|---|---|
| SYNC-1 | Working copy was 1 commit behind `origin/nextjs`, missing v2.378's polling denylist. Both scheduled jobs (`com.kabatone.seo-weekly`, `com.kabatone.seo-geo`) run with `WorkingDirectory` set to this copy, so the Monday Slack brief, the dashboard and the weekly report were all scored by the superseded scorer, which ranked a rank-tracking bot #1. fc4db54's own commit body predicted this on 2026-09-08. | 2026-09-14 | Claude | **resolved 2026-09-14** | Rebased onto `origin/nextjs`; conflicts were CHANGELOG.md / carry-over.md / changelog.html only, as predicted. Scorer verified by execution post-rebase: `best fire computer aided dispatch software` → `polling` / 0.0. |
| KAB-1721 | Track C escalation — earn real update history on the two flagship VMS pages | 2026-07-27 | Claude | **reframed 2026-09-08** | **Re-owned 2026-08-31.** This was logged for six weeks as "blocked on Omer's decision". It was not: CHANGELOG v2.286 records board approval via KAB-1980 ("both") on 2026-07-27. Track A ran (roundup program v2.277–v2.284); Track C's first move (freshness/E-E-A-T) shipped the same day. v2.286 set its own trigger — escalate "if this doesn't move the needle within 2–3 weekly pulls". `/resources/what-is-video-management-software` went 17.4 → 17.5 across the 08-20 and 08-28 pulls on ~18.6K impressions. The trigger fired weeks ago and nobody pulled the lever. **Reframed 2026-09-08 — the mechanism was wrong.** `what-is-video-management-software` **is** the page that ranks for every VMS query, buyer terms included (`best vms software`, `best video management software`), and it already holds 65 inbound internal links; `best-vms-software` draws 17 impressions at pos 24.2 on 7 links. So this is not a links problem and not a content problem — the page ranks **13.8** on `vms software` with 1,182 impressions and 6 clicks. It is an authority problem and belongs with AUTH-1/AUTH-2. A links re-point here would have stripped the one page that ranks. |
| KAB-1721-CLOSED | Track C escalation — earn real update history on the two flagship VMS pages | 2026-07-27 | Claude | open | **Re-owned 2026-08-31.** This was logged for six weeks as "blocked on Omer's decision". It was not: CHANGELOG v2.286 records board approval via KAB-1980 ("both") on 2026-07-27. Track A ran (roundup program v2.277–v2.284); Track C's first move (freshness/E-E-A-T) shipped the same day. v2.286 set its own trigger — escalate "if this doesn't move the needle within 2–3 weekly pulls". `/resources/what-is-video-management-software` went 17.4 → 17.5 across the 08-20 and 08-28 pulls on ~18.6K impressions. The trigger fired weeks ago and nobody pulled the lever. |
| AUTH-2 | `911 dispatch software for emergency call centers` — absent from AI answers, but **not** a content gap | 2026-08-31 | Omer | blocked | Audited the page: NG911, ANI/ALI, call-taking and text-to-911 are all already covered. Content is not the constraint — we are missing from the sources the engine retrieves, which cites CentralSquare, Hexagon, Mark43 and Motorola. This is AUTH-1 with a named target rather than a general ask: the four queries where we rank and are uncited are where authority should be spent. **2026-09-14: FLIPPED TO CITED** in the GEO run with no authority spent — the premise that this was blocked on backlinks may be wrong. Re-examine before allocating budget. |
| ~~CAD-1~~ | Re-point CAD cluster internal links to `best-cad-dispatch-software` | 2026-08-31 | Claude | **CLOSED 2026-09-22 — SHIPPED TO PRODUCTION** | Built 2026-09-08 as `seo/cad-cluster-link-consolidation`, then sat as **PR #18 for 14 days**. Merged to `main` 2026-09-22 (`ca26cda`); Vercel production deploy `dpl_8npztCkNce5QuUa5Xp4LkixVgdxP` **READY**. **Verified live on kabatone.com after deploy** — every buyer link flipped: `/vs/tyler-technologies` what-is 2 -> **0**, best 0 -> **2**; `/resources/best-public-safety-software` 3 -> **0**, 0 -> **3**; `/industries/public-safety` 2 -> **0**, 0 -> **2**. Source-wide the split went 60/36 (what-is/best) to 36/61. Measurement contract: buyer links now point at `best-cad-dispatch-software` (pos 9.4, 19,249 impr, 47 clicks) instead of `what-is-cad-dispatch-software` (pos 30.2, 0.21% CTR). **Expect movement on the CAD cluster over the next 2-3 pulls, not immediately** — PR #17's equivalent took three weeks to compound. Compare against the 12-clicks-flat baseline recorded 2026-09-22. |
| PLAN-2 | PR #7 — "Document the weekly brief pipeline and open items (v2.311)" | 2026-08-04 | Claude | **CLOSED UNMERGED 2026-09-22** | Do not reopen or re-apply. It was `MERGEABLE`/`CLEAN`; closing was a decision, not a block. It added 54 lines to `SEO/kabatone-seo-master-plan.md` (the file that seeds future sessions) carrying three disproved claims. (a) **It instructed deleting the `kabatone-seo-desktop` OAuth client** on the grounds that "nothing in the weekly path uses OAuth" — false, and it would break every weekly GSC pull. Verified path: `gsc_pull_weekly.py` -> `~/.claude/skills/seo/scripts/gsc_query.py:43 get_oauth_credentials()` -> `google_auth.py:26 TOKEN_PATH=~/.config/claude-seo/oauth-token.json`, refreshed via `refresh_token` at `google_auth.py:255`. Same wrong claim as PLAN-1, closed 2026-09-01. Its "regression test" hid that very token file. (b) Told a reader to retire `com.kabatone.seo-weekly.plist` as a superseded monolith — it is loaded and produces the Monday brief. (c) Reframed `c5` as "the standing weakness" (0.24% vs ~4% expected) — the intent-blind framing fixed in v2.330/BRIEF-1; `query_intent('c5')` = navigational, value 0.2, qualified potential 0. Its two genuinely useful findings (dimensionless GSC totals; row limit must exceed query count) are already live in the scripts, so nothing was lost. |
| ~~MIGRATE-1~~ | Unslashed canonical migration in progress | 2026-08-31 | Claude | **CLOSED 2026-09-22** | Mechanism verified correct end to end, so there is nothing left to watch: `curl -L` shows slashed -> **301** -> unslashed in a single hop on both pairs; canonicals on both forms point at the unslashed URL; **206 of 206** internal `/resources/` links in source are already unslashed (zero slashed). The handover cost 49 clicks this week -- `cctv-video-analytics` combined 149 -> 105, `best-cad-dispatch-software` 61 -> 56 -- against a site total of -47, so it accounts for the entire weekly decline. Impressions transferred near-perfectly (cctv: -3,072 slashed vs +3,196 unslashed) while clicks lagged, which is ordinary handover behaviour. Both surviving URLs are growing (+7 and +12 clicks) and holding position (p14.8->15.0, p9.5->9.4). No defect, no action. |
| AUTH-1 | Backlink / authority program | 2026-07-20 | Omer | blocked | Genuinely blocked — needs budget and human outreach. Scarcest resource in the program; do not spend on `c5`. Park until production catches up, so the shipped work can be measured before authority is bought. |
| GEO-4 | C5 definition corrected — does the AI answer flip? | 2026-09-01 | Claude | watching | v2.347 fixed the fifth C from Calidad to **Contacto Ciudadano** across 5 pages, both locales, FAQPage schema, meta descriptions and `llms.txt`. `What is a C5 command center?` has been uncited since the 2026-07-07 baseline and v2.274's brand-anchor callout did not flip it — the mechanism was that our definition contradicted `c5.cdmx.gob.mx`. Same measurement contract as v2.343: re-run `track_geo.py` after this reaches production and the engines re-crawl. **Citation is the metric, not rank.** The head term `c5` stays navigational (qualified potential 0) — do not read this as a click play. |
| GEO-2 | Split `track_geo.py` cited flag into `named_in_answer` vs `in_source_set` | 2026-08-28 | Claude | open | The 83% counts "our URL was retrieved" identically to "the answer recommended us". Until split, treat the rate as directional and keep it out of board decks as a bare percentage. |
| GEO-3 | `_BUYER` matches quoted-literal academic queries (`"traffic management system" "11 ktco2e"` scores buyer) | 2026-08-31 | Claude | open | Low value (score 9.6) but it will keep surfacing in the opportunity table. Needs a quoted-literal detector in `query_intent`. |
| SCORER-1 | Opportunity scorer ranks automated SERP-polling queries #1 | 2026-09-08 | Claude | open | `best fire computer aided dispatch software` ranked **#1** this week at 120 "qualified" potential clicks. It is polling, not demand: 2,007/2,010 impressions desktop, **zero clicks at pos 6.8**, country spread usa/gbr/nld/deu/ita/hkg with **zero Mexico**. CHANGELOG already documents this exact query — unfiltered, it drove **3 of the 4 P0 actions in the 2026-08-04 brief**. Fingerprint is mechanical (≈100% desktop + 0 clicks at page-1 + fixed country spread). Fix in `business_value()`/`query_intent()` so the Slack brief, dashboard and weekly report all correct at once. **Shipped v2.378.** Implemented as an evidence-backed denylist plus `verify_serp_polling()`, deliberately **not** a threshold: "page-1 position, high impressions, zero clicks" also describes genuine AI-Overview absorption (16 real queries this pull), so a threshold would have suppressed one of the programme's most important findings. Verified by execution — the query now returns intent `polling`, value `0.0`. |
| GEOSCHED-3 | GEO citation rate averaged two runs on the same date | 2026-09-08 | Claude | **closed 2026-09-08 (v2.378)** | **Filed wrong, corrected before shipping.** First raised as "the v2.340 double-run bug recurred". It did not: `geo-history.csv` held **24** doubled rows at 08:07 (`fa2a289`) and **12** by 09:04 (`4e3a3af`) — v2.340's de-duplication worked as claimed. The 37 rows came from `geo-queries.txt` growing 14→42 lines at 13:31 (`9fa95d9`, v2.342) and the expanded 25-query set running on top of the 12 already logged: two honest runs, one date. The real defect is that the history carries **no run identifier**, so row-count arithmetic averaged a 12-query run with a 25-query run and published 73.0% — a figure belonging to neither. `geo_freshness()` now dedupes by `(date, query)`, last run wins, and reports `superseded_rows`. Verified by execution: 08-31 = **68.0%** (17/25), 12 superseded. Like-for-like on the stable 12 is 83.3% in both runs — flat, not a 10-point fall. No paid spend was wasted. |
| TOOL-1 | `gsc_pull_weekly.py` was untracked and lost with the working copy | 2026-09-08 | Claude | open | The OneDrive working copy is unreadable (`ETIMEDOUT`, `st_blocks=0`, 12,602/13,304 files dehydrated), and the pull script existed **only there** — it is not in git. Reconstructed in the `~/dev/k1-website` clone and validated by replaying the 09-01 window: striking-distance count reproduced **exactly** (101), pages 20/20, queries 29/30, all differences traced to GSC backfill. Needs committing. Two notes: the reconstruction authenticates via `gsc_query.py` (service account) rather than the original's refresh-token path — see PLAN-1 — and `top_*_by_impressions` are in fact sorted by **clicks**; the misnomer was preserved deliberately so stored pulls stay comparable. **Shipped v2.378** — committed to `nextjs`. |

## Closed

| id | item | first_raised | closed | shipped_in |
|---|---|---|---|---|
| GEOSCHED-2 | GEO monitor ran twice every Monday | 2026-08-31 | 2026-08-31 | **v2.340.** `com.kabatone.seo-geo` (07:30) and `seo_weekly_agent.py`'s own `geo-monitor` step (08:07) both shelled out to `track_geo.py` — 24 rows for a 12-query set, double paid spend, citation rate computed from doubled rows. `launchctl` showed one job because the second lived inside another script. Fixed by making the agent skip when the day's rows exist, so the duplicate run goes without losing the fallback. Verified by execution. Rows de-duplicated; both runs had agreed 10/12, so 83.3% was accidentally correct. |
| WATCH-1 | `/k-dispatch/` position decline | 2026-08-28 | 2026-08-31 | **Withdrawn, not fixed — there was no decline.** Queried GSC directly (the stored pulls' top-20 cap hid it): clicks **flat at 10** across both 28d windows, CTR **0.43% → 0.71%**. Lost impressions were mid-tail terms held at pos 45–84 that never converted; the page moved to **pos 1–4** on `911 cad`, `emergency cad`, `911 dispatcher software`. Average position fell because the query mix changed. v2.282 reached the same conclusion in July ("healthy query reallocation — no fix"); re-escalating it cost a week of queue space. EN CAD cluster grew 68 → 82 clicks (+21%). |
| VID-1 | Consolidate video-analytics cluster | 2026-08-12 | 2026-08-31 | v2.329 (staging) → **v2.376 / PR #17 (production, 2026-08-31)**. 3 weeks carry-over. Mechanism: 28 internal links pointed at the dead page vs 8 at the winner. Verified live: both retired URLs 301 in 1 hop, winner 200, `best-ai-video-analytics-software` correctly untouched. |
| SHIP-1 | Get the SEO fixes onto production | 2026-08-21 | 2026-08-31 | **PR #17 merged to `main`.** Not a merge of `nextjs` — the branches had diverged (main 163 commits ahead at a higher version), so the changes were re-applied on a branch cut from `main` and resolved against its newer content. Deploy verified live ~7min after merge. |
| CANON-2 | `psim-alternatives` canonicals pointed at a 308 | 2026-08-28 | 2026-08-31 | v2.331 (staging) → v2.376 / PR #17 (production). Verified live: canonical now unslashed. |
| STATE-1 | `SEO/SEO-PROGRAM-STATE.md` opens with stale 🔴 warnings | 2026-08-31 | 2026-09-01 | **Already fixed when checked — the ledger row was the stale thing.** The file now carries a struck-through "Resolved, do not re-raise" block covering both the dry Anthropic key and the `gsc_reauth.py` claim. Verified by reading the file, not by trusting the row. Superseded by PLAN-1, which found the same class of rot one file over. |
| PLAN-1 | `kabatone-seo-master-plan.md` was 20 days stale and wrong on two counts | 2026-09-01 | 2026-09-01 | **Closed same day.** (a) Its status block asserted "C5 is an authority problem, not content" — the diagnosis this week disproved, and the one that cost six weeks. (b) Its header claimed analytics auth is a **service account** with "no OAuth refresh token used in the weekly path"; `gsc_pull_weekly.py` in fact exchanges a refresh token at `oauth2.googleapis.com/token`. A `gsc-service-account.json` does exist in `~/.config/claude-seo/`, which is why the wrong claim survived inspection — checking which credential files exist does not tell you which one the code reads. Verified by reading the auth path. |
| GEO-1 | Add ES + PT queries to `SEO/geo/geo-queries.txt` | 2026-08-28 | 2026-09-01 | **v2.342** — shipped 2026-08-31; the ledger row was stale for a day. Query set went 12 → 25 in three labelled blocks (original 12 for trend continuity, the zero-click block, ES/PT). The split immediately paid: real citation rate is **68%, not 83%** — the old set only tested queries we already win. ES/PT came back 4 of 6 cited, healthier than feared. |
| CANON-1 | Unslash all canonicals | 2026-08-10 | 2026-08-21 | v2.314, promoted 08-21; two stragglers swept in v2.331 |
| GEOSCHED-1 | Restore + schedule GEO monitor | 2026-08-20 | 2026-08-28 | v2.328 |
| BRIEF-1 | Slack brief headlines `c5` despite correct ranking | 2026-08-28 | 2026-08-28 | v2.330 — intent labels + qualified potential |
