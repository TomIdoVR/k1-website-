# KabatOne SEO Audit — 2026-05-28 14:46

**Auditor:** `kabatone-seo-audit` skill (wraps `claude-seo` v2.0.0)
**Target:** https://kabatone.com/ (production)
**Branch state:** local at v2.207 (`3b17c15`), `origin/nextjs` at v2.205 (`ba6b41a`), `origin/main` at v1.53 (`950b5f2`, 2026-04-08)

---

## Executive summary

**Health score: 42 / 100** — heavily dragged down by a single, dominant P0.

| Severity | Count | Headline |
|---|---|---|
| **P0** | 3 | Production is 259 commits / 50 days behind staging. 121 of 125 country pages return 404 on kabatone.com. 24 dead URLs in production sitemap. |
| **P1** | 4 | GSC service account not yet shared on property (audit blind to performance data). 2 unpushed staging commits. GEO citation gap on "best public safety software municipalities LATAM 2026". Empty Bahrain directory in source. |
| **P2** | 2 | Master plan freshly synced (no drift). Homepage signals are clean. |

**Bottom line:** the entire SEO sprint of the past 50 days has had **zero production SEO impact**. Country pages, metadata fixes, internal-linking improvements, GEO guides — all built, all sitting on `nextjs` (staging), none live on `kabatone.com`. Production sitemap still advertises 24 URLs that 404. This is the only finding that matters until it's resolved.

---

## P0 findings (must-fix, blocking)

### P0-1 — Production frozen for 50 days

| | |
|---|---|
| **What** | `main` branch HEAD is `950b5f2` from 2026-04-08 (v1.53). `nextjs` is 259 commits ahead. Production (`kabatone.com`) reflects only what was on `main` on April 8. |
| **Why it matters** | Every page, schema improvement, metadata fix, and GEO guide built between v1.53 and v2.207 — including the entire country-page program, the bulk metadata length corrections, and Next.js 16.2.6 — exists only on staging. None of it is indexable, citable, or visible to Google. |
| **Evidence** | `git log origin/main -1`: 2026-04-08 v1.53. `git log origin/nextjs -1`: 2026-05-25 v2.205. `git rev-list --count origin/main..origin/nextjs`: 259. |
| **How to fix** | Promote `nextjs` → `main`. Either via PR (`gh pr create --base main --head nextjs`) or `git checkout main && git merge --ff-only nextjs && git push origin main`. Vercel will auto-deploy. **Verify the build is green on staging first** — the v2.205 commit was a build fix, so confidence should be high. |
| **Effort** | 5 minutes (merge + 3-min Vercel deploy + smoke check). |

### P0-2 — 121 of 125 country pages return 404 on production

| | |
|---|---|
| **What** | Only 4 country pages are live on `kabatone.com`: Colombia, Mexico-municipalities, Peru, Small-cities. The other 121 (Mexico-country, United States, Brazil, Argentina, all of Europe, Africa, Middle East, APAC) return HTTP 404. |
| **Why it matters** | Months of content production has zero search exposure on production. Any internal link from the resources hub or sitemap pointing at these URLs is a dead link. Any GSC discovery/crawl attempt on these URLs increments the "Crawled — currently not indexed" or "404" counters. |
| **Evidence** | Parallel HTTP HEAD probe of all 125 country slugs against `kabatone.com/resources/public-safety-software-<slug>/`. 121 returned 404, 4 returned 200 with substantive content. Raw results: `SEO/audits/raw-country-coverage-2026-05-28-1446.json`. |
| **How to fix** | Resolved automatically by P0-1 (promote nextjs → main). |
| **Effort** | Subsumed by P0-1. |

### P0-3 — 24 dead URLs in production sitemap

| | |
|---|---|
| **What** | `https://kabatone.com/sitemap.xml` advertises 158 URLs. 32 of those are `public-safety-software-*` country-page URLs (16 unique × 2 locales). Only 4 unique slugs are live, so 12 unique × 2 locales = 24 sitemap URLs point to 404s. |
| **Why it matters** | A sitemap full of 404s tells Google the site is poorly maintained. It also wastes crawl budget — Googlebot will re-try these URLs repeatedly. GSC will likely show "Submitted URL not found (404)" errors against the property. |
| **Evidence** | `curl https://kabatone.com/sitemap.xml | grep -c "<loc>"` → 158. `grep -c "public-safety-software-"` → 32. Cross-referenced against the production live-page check above. |
| **How to fix** | Also resolved by P0-1 — `src/app/sitemap.ts` on `nextjs` lists the full 204 routes; promoting nextjs → main makes the sitemap match reality. |
| **Effort** | Subsumed by P0-1. |

---

## P1 findings (should-fix, ~30 days)

### P1-1 — GSC service account not yet shared on property

| | |
|---|---|
| **What** | Service account `kabatone-seo-reader@kabatone-seo.iam.gserviceaccount.com` is authenticated (Tier 1 confirmed via `google_auth.py --check`) but `gsc_query.py sites` returns `{"sites": []}` — no properties shared with the account. |
| **Why it matters** | This audit is blind to: organic clicks/impressions, query data, CTR by page, ranking gains/losses, indexation status from Google's perspective, country/device breakdown, week-over-week deltas. Roughly 50% of a "professional" audit's value depends on GSC. |
| **How to fix** | Open https://search.google.com/search-console → pick `kabatone.com` property → Settings → Users and permissions → Add user → paste `kabatone-seo-reader@kabatone-seo.iam.gserviceaccount.com` → permission: **Restricted** (read-only is fine for audits) → Add. Wait ~60 sec, then re-run this audit. |
| **Effort** | 2 minutes. |

### P1-2 — 2 unpushed staging commits

| | |
|---|---|
| **What** | Local `HEAD` is at `3b17c15` (v2.207). `origin/nextjs` is at `ba6b41a` (v2.205). v2.206 (Honduras GEO guide) and v2.207 (VMS guide) are committed locally but not pushed. |
| **Why it matters** | Staging doesn't reflect the latest work. If you promote `nextjs` → `main` (P0-1) right now without pushing first, those two pages won't make it to production. |
| **How to fix** | `git push origin nextjs` (you'll need to approve the push per your standing rule). |
| **Effort** | 1 minute. |

### P1-3 — GEO citation gap on key non-branded query

| | |
|---|---|
| **What** | Web search for "best public safety software municipalities Mexico Latin America 2026" returned only **one** kabatone.com result (the small-cities page). Competitors (Seguritech, SafetyCulture, FeaturedCustomers, generic listings) dominated the SERP. Branded queries are fully owned (10/10 results for "KabatOne public safety platform"). |
| **Why it matters** | This is the highest-value non-branded query for a B2G LATAM ICP. Buyers asking AI assistants for shortlists on this query are not consistently being shown KabatOne. The country pages were *supposed* to address this — but they're 404 on production (see P0-2). |
| **How to fix** | Promote nextjs → main (P0-1) — the country pages are the answer. Then re-run the citation check in 2–3 weeks once Google has crawled. If still missing, deepen the country-page content (current micro-market pages are templated; competitors win with depth). |
| **Effort** | Subsumed by P0-1 + future content depth pass. |

### P1-4 — Empty `public-safety-software-bahrain/` directory in source

| | |
|---|---|
| **What** | `src/app/[locale]/resources/public-safety-software-bahrain/` exists as an empty directory (no `page.tsx`), untracked by git. Leftover from an aborted auto-generation. |
| **Why it matters** | Cosmetic / hygiene. Harmless on its own (Next.js won't route an empty dir) but pollutes the directory listing and confuses future audits. |
| **How to fix** | `rmdir "src/app/[locale]/resources/public-safety-software-bahrain"` |
| **Effort** | 10 seconds. |

---

## P2 findings (nice-to-have)

### P2-1 — Hreflang served via HTTP `Link` headers, not HTML `<link>` tags

Detected in the response headers as `<https://www.kabatone.com/>; rel="alternate"; hreflang="en"` + `es` + `x-default`. This is **valid per Google's spec** and equivalent to HTML tags. The earlier "0 hreflang count" alarm during the probe was a false positive because the probe only looked at HTML, not headers. No action needed.

### P2-2 — Homepage signals are clean

Title 73 chars (acceptable for branded homepage), description 158 chars (under Google's 160 limit), canonical present, 4 JSON-LD schema scripts, page size 222 KB (reasonable). No on-page issues to fix on the homepage.

---

## GSC performance snapshot

**BLOCKED** — see P1-1. Re-run audit after sharing the property with the service account to populate this section. The skill will pull: top 20 queries (impressions/clicks/CTR/position), week-over-week deltas, biggest gains/losses, indexation breakdown (indexed / discovered-not-indexed / crawled-not-indexed / errors), country/device breakdown.

## GEO / AI citation snapshot

Spot check on 2 prompts (full GEO sub-skill not yet exercised — first run):

| Prompt | KabatOne cited? | Position | Competitors cited |
|---|---|---|---|
| "KabatOne public safety platform CAD dispatch C5 command center" | ✅ Yes | 1–10 dominated | None — full brand ownership |
| "best public safety software municipalities Mexico Latin America 2026" | ⚠️ Once | One result (small-cities page) | Seguritech, SafetyCulture, FeaturedCustomers, generic SaaS listings |

**Read:** brand search is bulletproof; non-branded high-value queries are weak. P0-1 unblocks the country pages that should close this gap.

## Country-page audit (TAM lens)

Holding this analysis until P0-1 is resolved. Once country pages are live on production, the wrapper skill will re-classify all 121 pages against the KabatOne TAM whitelist (core / strategic / marginal / at-risk micro-markets) and produce a keep/review/cut list. Running it now would be premature because the data we'd analyze isn't yet what Google sees.

## Master plan drift

✅ Clean. Master plan was synced 2026-05-19 (route count, GEO tracker, autopilot flag, Bahrain note). Nothing to reconcile.

---

## Recommended next 5 actions (in order)

1. **Push the 2 staged commits** (`git push origin nextjs`) — 1 minute. Ensures v2.206 + v2.207 make it to staging.
2. **Promote `nextjs` → `main`** — 5 minutes including Vercel deploy. **This single action closes P0-1, P0-2, P0-3 simultaneously.** After deploy: re-probe 10 country pages to confirm they're live.
3. **Share GSC property with service account** — 2 minutes. Unblocks the performance half of the next audit.
4. **Re-run this audit** after #1, #2, #3 complete. The next report will be substantially more useful (will include GSC data, real country-page analysis, GEO gap re-test against deployed pages).
5. **Delete `public-safety-software-bahrain/` empty dir** — 10 seconds. Hygiene.

**Total time to clear all P0+P1 actionable items: ~12 minutes of your time, plus Vercel deploy wait.**

---

## Methodology

- **Source data:** local git state, file system enumeration of `src/app/[locale]/resources/`, parallel HTTP probes against production (`kabatone.com`) for 125 country slugs, production sitemap fetch, robots.txt probe, homepage HTML signal extraction, 2 web search citation checks.
- **Skills exercised:** `kabatone-seo-audit` (wrapper) + manual delegation to underlying claude-seo scripts (`gsc_query.py`, `google_auth.py`).
- **Skills NOT exercised this run:** `seo-technical` subagent (would add deeper crawl + schema validation), `seo-google` subagent (blocked by P1-1), `seo-content` subagent (premature given P0-1), `seo-geo` subagent (will run more thoroughly post-P0-1), `seo-drift` subagent (no baseline exists yet — will be captured next run).
- **Base claude-seo audit (500-page crawl) skipped:** with production serving 404 for 121 of 154 resources URLs, crawling production would generate noise without insight. Local filesystem analysis was more accurate.

## Audit run log

```
| 2026-05-28 14:46 | kabatone-seo-audit | first-run / coverage map | https://kabatone.com/ | SEO/audits/2026-05-28-1446-audit.md | Health: 42/100 | P0: 3 | P1: 4 | P2: 2 |
```
